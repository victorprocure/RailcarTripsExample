# RailcarTrips

## Dependencies
### System
  - Docker Desktop, not required if you're using a local SQL install. You will however need it if setting up SQL via a container, as laid out in the [database guide](./guides/sql-server.md).
  - SQL Server, LocalDB should be fine, however I only tested on a developer server setup via docker-compose
  - Dotnet 10
